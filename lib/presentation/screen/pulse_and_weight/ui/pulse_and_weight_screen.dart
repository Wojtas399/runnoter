import 'package:flutter/material.dart';

class PulseAndWeightScreen extends StatelessWidget {
  const PulseAndWeightScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pulse & Weight screen'),
    );
  }
}
