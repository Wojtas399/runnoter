import 'package:flutter/material.dart';

class PaceUnitScreen extends StatelessWidget {
  const PaceUnitScreen({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Pace unit screen'),
      ),
    );
  }
}
