import 'package:flutter/material.dart';

class Gap8 extends StatelessWidget {
  const Gap8({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox(height: 8);
}

class Gap16 extends StatelessWidget {
  const Gap16({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox(height: 16);
}

class Gap24 extends StatelessWidget {
  const Gap24({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox(height: 24);
}

class Gap32 extends StatelessWidget {
  const Gap32({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox(height: 32);
}

class Gap40 extends StatelessWidget {
  const Gap40({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox(height: 40);
}
